═══════════════════════════════════════════════════════════════════════════════
 TC.PAT.FLOW.008 — Upload icon navigates to /admin/clinics/{id}/patients/import AND the page exposes three controls — file picker (#import_csv_file), "Upload CSV" submit button, "Download Template File" link (href ends /csvTemplate). Actual upload behaviour (CSV parsing, error rows, etc.) is a placeholder for a future UPLOAD sub-plan.
═══════════════════════════════════════════════════════════════════════════════
Sub-feature : Patient lifecycle — cross-feature integration (PAT.FLOW)
Section     : [Functionality]
Tags        : <TBD> <AUTOMATION>
Status      : STUB — see [Steps] block below for status of detailed reproduction

[Preconfiguration]
  Account    = super-user (demo / Beautify Clinic, id=3)
  Browser    = Chrome (latest stable)
  Steps      = 1. Open https://staging.consentz.com/admin/login
               2. Log in with CONSENTZ_USERNAME / CONSENTZ_PASSWORD.
               3. Wait for /admin/clinics/3/dashboard.
               4. Dismiss "Welcome to Consentz" modal if present.

[Steps]
  STUB — to be expanded on first manual execution.
  Intent: Upload icon navigates to /admin/clinics/{id}/patients/import AND the page exposes three controls — file picker (#import_csv_file), "Upload CSV" submit button, "Download Template File" link (href ends /csvTemplate). Actual upload behaviour (CSV parsing, error rows, etc.) is a placeholder for a future UPLOAD sub-plan.

[Expected]
  STUB — to be filled in alongside the [Steps] block above.

[Postconfiguration]
  NA — case does not modify persistent state.
