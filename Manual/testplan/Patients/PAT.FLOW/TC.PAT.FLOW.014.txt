═══════════════════════════════════════════════════════════════════════════════
 TC.PAT.FLOW.014 — Patient module endpoint sweep — for every known patient-scoped URL (list, /new, /import, /csvTemplate download, /export download, /merge, /{id}/edit, /{id}/correspondence, /{id}/appointments, /{id}/notes, /{id}/documents, /{id}/correspondence/new) navigate the browser, assert HTTP status < 400, assert a visible content element renders (assertPageLoaded), AND assert the page-noise listener observes no console errors / no 4xx / no 5xx / no UI error flashes. Also exercise search-input permutations on the list page: empty submit, single character, very long (>200 chars), special chars (XSS-shaped <script>, SQL-shaped ', emoji, leading + trailing whitespace), exact phone match. Each permutation must NOT throw 4xx/5xx and the list must re-render without console errors.
═══════════════════════════════════════════════════════════════════════════════
Sub-feature : Patient lifecycle — cross-feature integration (PAT.FLOW)
Section     : [Negative tests]
Tags        : <TBD> <AUTOMATION>
Status      : STUB — see [Steps] block below for status of detailed reproduction

[Preconfiguration]
  Account    = super-user (demo / Beautify Clinic, id=3)
  Browser    = Chrome (latest stable)
  Steps      = 1. Open https://staging.consentz.com/admin/login
               2. Log in with CONSENTZ_USERNAME / CONSENTZ_PASSWORD.
               3. Wait for /admin/clinics/3/dashboard.
               4. Dismiss "Welcome to Consentz" modal if present.

[Steps]
  STUB — to be expanded on first manual execution.
  Intent: Patient module endpoint sweep — for every known patient-scoped URL (list, /new, /import, /csvTemplate download, /export download, /merge, /{id}/edit, /{id}/correspondence, /{id}/appointments, /{id}/notes, /{id}/documents, /{id}/correspondence/new) navigate the browser, assert HTTP status < 400, assert a visible content element renders (assertPageLoaded), AND assert the page-noise listener observes no console errors / no 4xx / no 5xx / no UI error flashes. Also exercise search-input permutations on the list page: empty submit, single character, very long (>200 chars), special chars (XSS-shaped <script>, SQL-shaped ', emoji, leading + trailing whitespace), exact phone match. Each permutation must NOT throw 4xx/5xx and the list must re-render without console errors.

[Expected]
  STUB — to be filled in alongside the [Steps] block above.

[Postconfiguration]
  NA — case does not modify persistent state.
