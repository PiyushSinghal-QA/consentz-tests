═══════════════════════════════════════════════════════════════════════════════
 TC.PAT.FLOW.003 — Add Patient — POSITIVE path: click Add Patient → fill required fields with marker prefix → click Save → redirected to /patients/{id}/edit → manually navigate back to /patients → search by marker → patient is in the list. Then NEGATIVE path: click Add Patient → fill same required fields → click Cancel → manually navigate back to /patients → search by a fresh marker → patient is NOT in the list
═══════════════════════════════════════════════════════════════════════════════
Sub-feature : Patient lifecycle — cross-feature integration (PAT.FLOW)
Section     : [Functionality]
Tags        : <TBD> <AUTOMATION>
Status      : STUB — see [Steps] block below for status of detailed reproduction

[Preconfiguration]
  Account    = super-user (demo / Beautify Clinic, id=3)
  Browser    = Chrome (latest stable)
  Steps      = 1. Open https://staging.consentz.com/admin/login
               2. Log in with CONSENTZ_USERNAME / CONSENTZ_PASSWORD.
               3. Wait for /admin/clinics/3/dashboard.
               4. Dismiss "Welcome to Consentz" modal if present.

[Steps]
  STUB — to be expanded on first manual execution.
  Intent: Add Patient — POSITIVE path: click Add Patient → fill required fields with marker prefix → click Save → redirected to /patients/{id}/edit → manually navigate back to /patients → search by marker → patient is in the list. Then NEGATIVE path: click Add Patient → fill same required fields → click Cancel → manually navigate back to /patients → search by a fresh marker → patient is NOT in the list

[Expected]
  STUB — to be filled in alongside the [Steps] block above.

[Postconfiguration]
  NA — case does not modify persistent state.
