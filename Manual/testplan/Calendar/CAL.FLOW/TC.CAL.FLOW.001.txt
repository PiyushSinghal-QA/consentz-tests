═══════════════════════════════════════════════════════════════════════════════
 TC.CAL.FLOW.001 — Navigation to Calendar via BOTH the top-bar Calendar icon (a.nav-link[title="Calendar"]) AND the side-bar Calendar link (.sidebar a[href$="/appointments/calendar"]): each click lands on /admin/clinics/{id}/appointments/calendar with the FullCalendar grid rendered (.fc-agendaWeek-button visible) AND the inline mini-datepicker (.datepicker.datepicker-inline) visible. Navigating back to the dashboard works between the two clicks.
═══════════════════════════════════════════════════════════════════════════════
Sub-feature : Calendar lifecycle — cross-feature integration (CAL.FLOW)
Section     : [Functionality]
Tags        : <TBD> <AUTOMATION>
Status      : STUB — see [Steps] block below for status of detailed reproduction

[Preconfiguration]
  Account    = super-user (demo / Beautify Clinic, id=3)
  Browser    = Chrome (latest stable)
  Steps      = 1. Open https://staging.consentz.com/admin/login
               2. Log in with CONSENTZ_USERNAME / CONSENTZ_PASSWORD.
               3. Wait for /admin/clinics/3/dashboard.
               4. Dismiss "Welcome to Consentz" modal if present.

[Steps]
  STUB — to be expanded on first manual execution.
  Intent: Navigation to Calendar via BOTH the top-bar Calendar icon (a.nav-link[title="Calendar"]) AND the side-bar Calendar link (.sidebar a[href$="/appointments/calendar"]): each click lands on /admin/clinics/{id}/appointments/calendar with the FullCalendar grid rendered (.fc-agendaWeek-button visible) AND the inline mini-datepicker (.datepicker.datepicker-inline) visible. Navigating back to the dashboard works between the two clicks.

[Expected]
  STUB — to be filled in alongside the [Steps] block above.

[Postconfiguration]
  NA — case does not modify persistent state.
