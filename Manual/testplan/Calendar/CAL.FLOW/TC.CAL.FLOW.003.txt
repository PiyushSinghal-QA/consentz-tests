═══════════════════════════════════════════════════════════════════════════════
 TC.CAL.FLOW.003 — Calendar interactivity: (a) clicking a non-today day cell on the mini-datepicker (.datepicker.datepicker-inline .day:not(.disabled,.old,.new)) updates the FullCalendar grid (the toolbar's currently-active view re-renders with the chosen date in scope); (b) clicking the "Today" button after navigating away returns the FullCalendar grid to today's date (today's day-cell carries an "active" / "today" class on the mini-datepicker); (c) clicking each of Day/Week/Month view-mode toggles renders the corresponding view without console errors.
═══════════════════════════════════════════════════════════════════════════════
Sub-feature : Calendar lifecycle — cross-feature integration (CAL.FLOW)
Section     : [Functionality]
Tags        : <TBD> <AUTOMATION>
Status      : STUB — see [Steps] block below for status of detailed reproduction

[Preconfiguration]
  Account    = super-user (demo / Beautify Clinic, id=3)
  Browser    = Chrome (latest stable)
  Steps      = 1. Open https://staging.consentz.com/admin/login
               2. Log in with CONSENTZ_USERNAME / CONSENTZ_PASSWORD.
               3. Wait for /admin/clinics/3/dashboard.
               4. Dismiss "Welcome to Consentz" modal if present.

[Steps]
  STUB — to be expanded on first manual execution.
  Intent: Calendar interactivity: (a) clicking a non-today day cell on the mini-datepicker (.datepicker.datepicker-inline .day:not(.disabled,.old,.new)) updates the FullCalendar grid (the toolbar's currently-active view re-renders with the chosen date in scope); (b) clicking the "Today" button after navigating away returns the FullCalendar grid to today's date (today's day-cell carries an "active" / "today" class on the mini-datepicker); (c) clicking each of Day/Week/Month view-mode toggles renders the corresponding view without console errors.

[Expected]
  STUB — to be filled in alongside the [Steps] block above.

[Postconfiguration]
  NA — case does not modify persistent state.
